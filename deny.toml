# cargo-deny configuration for security and license management
# This is a minimal working configuration for modern cargo-deny

[graph]
targets = []

[output]
feature-depth = 1

[advisories]
db-path = "~/.cargo/advisory-dbs"
db-urls = ["https://github.com/rustsec/advisory-db"]
ignore = [
    # bincode 1.3.3 is unmaintained (RUSTSEC-2025-0141), used transitively via
    # syntect. No patched version exists; the fix requires syntect to migrate to
    # bincode 2.x. Accepted until syntect publishes a release with bincode 2.
    "RUSTSEC-2025-0141",
]

[licenses]
confidence-threshold = 0.8
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-DFS-2016",
    "Unicode-3.0",
    "CC0-1.0",
    "MPL-2.0",
]

[bans]
multiple-versions = "warn"
wildcards = "allow"
highlight = "all"
allow = []
deny = []

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
