name: Code Quality & Security

on:
  schedule:
    - cron: "0 2 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    uses: docdyhr/.github/.github/workflows/rust-quality.yml@45a390baa5d5e6b3cd058b0d16023410297ede84 # v1
    secrets: inherit # pragma: allowlist secret
